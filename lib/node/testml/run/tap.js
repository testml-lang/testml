// Generated by CoffeeScript 2.2.4
(function() {
  var Run;

  require('../../testml/run');

  module.exports = Run = class extends TestML.Run {
    static run(file) {
      return (new Run(file)).test();
    }

    constructor() {
      super(...arguments);
      this.tap = new require('tap');
    }

    test_begin() {}

    test_end() {
      return this.tap.done();
    }

    test_eq(got, want, label) {
      return this.tap.equal(got, want, label);
    }

  };

}).call(this);
